import { styled } from "..";

export const AvatarContainer = styled('img', {
    width: '$12',
    height: '$12',
    borderRadius: '$full',
    cursor: 'pointer',

})

export const DefaultAvatar = styled('div', {
    width: '$12',
    height: '$12',
    borderRadius: '$full',
    cursor: 'pointer',

    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',

})