import { ComponentProps } from "react";
import { SocialsContainer } from "../../styles/pages/socials";

export interface SocialsProps {
    linkedinlink?: string,
    spotifylink?: string,
    githublink?: string,
    instagramlink?: string,
    twitterlink?: string,
    blueskylink?: string,
    facebooklink?: string,
    redditlink?: string,

    size?: string,
}